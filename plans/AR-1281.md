# AR-1281: Supervised cassette lifecycle execution

## Outcome

Implement the complete supervised cassette execution path for the primary argument-level replay
command from protected main, including runtime handoff and lifecycle fault handling.

## Dependencies and ownership

Dependencies: AR-1237, AR-1238, AR-1239. AR-1275 through AR-1280 are blocked evidence only; do not
reuse their unmerged branches. Owned paths: cross-crate replay transport/process entrypoint,
primary CLI dispatch, runtime supervisor, cassette fixtures, and executable lifecycle tests.
Branch: `feature/ar-1281-supervised-cassette-lifecycle`; worktree:
`agent-systems-benchmark-ar-1281-supervised-cassette-lifecycle`.

## Acceptance

The real command receives authenticated runtime authority and executes cassette request/response in
a supervised process. Prove provider/descendant egress denial, no live fallback,
cancellation/restart, timeout/crash reaping, cleanup, malformed/stale/duplicate rejection, and
bounded evidence. Run full locked gates, independent review, exact-head CI, signed merge, and
post-merge assurance. Do not substitute an audit-only result.
