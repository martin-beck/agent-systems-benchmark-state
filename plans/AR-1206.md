# AR-1206: Publish the integrated CLI surface and workflow documentation

## Outcome

Bring `doctor`, `capabilities`, help, completion, README, PLAN, and the workflow/quickstart guides
in line with the integrated record/replay/benchmark surface, and regenerate the checked workflow
transcripts.

## Dependencies and ownership

Dependencies: AR-1202, AR-1203, AR-1204, AR-1205. Repository:
`martin-beck/agent-systems-benchmark` only. Owned paths are `README.md`, `docs/PLAN.md` (command
list), `docs/workflows/record-replay.md`, `docs/QUICKSTART.md`, `docs/REPRODUCIBILITY.md`,
`crates/asb-cli` help/completion/`doctor`/`capabilities`, and the `guide_examples.rs` and
`workflow_transcript.rs` fixtures. Branch: `feature/record-replay-docs`. Worktree:
`agent-systems-benchmark-record-replay-docs`.

## Work sequence

1. Correct stale claims: the README "no record/replay command" paragraph, the PLAN command list
   (`asb record EXPERIMENT.toml`, `asb replay RUN`), and help/completion text.
2. Document the exact integrated journey (AR-1204) with CLI, TUI route and noninteractive forms,
   evidence boundaries, and OpenRouter free-model setup.
3. Regenerate and commit checked transcripts; update `doctor`/`capabilities` outputs to the
   implemented surface so planned commands cannot appear as working features.

## Acceptance criteria

All documentation matches the implemented commands; every guide command is executable and verified
(offline via AR-1207 double where applicable); no stale or contradictory claims remain;
`capabilities` reflects reality.

## Evidence

Regenerated transcripts, guide-execution CI, doc/link checks, exact-head CI.
