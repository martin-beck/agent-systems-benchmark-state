# AR-0905: Model execution recovery and worker fencing

## Outcome

Apply bounded formal models to run lifecycle, leases, recovery, replay cursors and uncertain external effects.

## Dependencies and ownership

Dependencies: AR-0102, AR-0104, AR-0204, AR-0503.
Owned paths: docs/formal models, verifiers, generated trace corpus and CI.
Branch: feature/recovery-models. Worktree: agent-systems-benchmark-recovery-models.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Translate Agent Relay's TLA+/Alloy/executable-model pattern to ASB run and replay domains.
2. Implement the smallest complete boundary and its independent failure tests.
3. Run focused and applicable full quality gates on the exact commit.
4. Publish a focused PR, reconcile CI and record verified limitations.

## Acceptance criteria

Pinned TLC and Alloy runs check declared finite scopes; independent executable state exploration covers named hostile traces; Rust implementation conforms through generated trace tests; invariants include unique lease, stale-event fencing, monotonic journal/projection, no duplicate accepted completion, replay-session isolation and explicit uncertain terminal state; bounds and assumptions are published.

## Evidence and completion

Record source/tool versions, commands by digest, immutable commit and CI identifiers,
observed outcomes and remaining limitations. No raw private configuration or transcripts.
Include negative cases and a real production-boundary integration where applicable.
Do not satisfy native evidence with mocks or emulation. Finish only after the requested
outcome, review, exact-head checks and post-merge checks pass.
