# AR-0307: Implement goose client adapter

## Outcome

Run a pinned Apache-2.0 AAIF goose CLI release through no-session structured output.

## Dependencies and ownership

Dependencies: AR-0101, AR-0102, AR-0103.
Owned paths: asb-agents goose module and its fixtures.
Branch: feature/agent-goose. Worktree: agent-systems-benchmark-agent-goose.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Inspect exact aaif-goose/goose source, license, release assets, JSON/stream-JSON dialect, custom OpenAI-compatible provider and tool/extension loading behavior.
2. Pin source and native asset digests for supported glibc/musl architectures; use no-session mode, isolated state, explicit provider/model, bounded turns and only declared built-ins.
3. Exercise real editing/tool/cancellation/failure behavior against a credential-free loopback provider and prove missing required extensions fail closed.
4. Run focused and full gates, publish a focused PR, reconcile exact-head CI and record limitations.

## Acceptance criteria

Real pinned goose binaries complete the common offline fixture with bounded structured lifecycle/tool/usage evidence on supported native Linux architectures. No ambient session, extension, credential, recipe or config affects execution; provider override, cancellation and cleanup are verified. GPU/Vulkan, subscription and untested asset combinations remain unsupported.

## Evidence and completion

Record exact source/tag/tree, license and release-asset digests, commands by digest, immutable commit, CI identifiers, native outcomes and unsupported combinations. No credentials, diagnostics bundles, raw prompts, transcripts or private paths. Finish only after independent immutable-head review, exact-head checks, integration and post-merge verification.

