# AR-1283: Repair formal lockfile CI drift

## Outcome

Repair the standalone formal workspace lockfile/provenance so the documented and hosted
`cargo test --locked --manifest-path formal/Cargo.toml` gate runs without attempting lock updates.

## Dependencies and ownership

Dependencies: AR-0003. Owned paths: `formal/Cargo.lock`, formal lockfile/provenance documentation,
and focused CI fixtures only. Branch: `feature/ar-1283-formal-lockfile`; worktree:
`agent-systems-benchmark-ar-1283-formal-lockfile`.

## Acceptance

Regenerate and validate the lockfile offline against exact path dependencies and pinned registry
sources; prove the hosted command succeeds with `--locked`, preserve privacy/signature/DCO, and run
the applicable full formal/repository gates. Do not weaken `--locked` or change formal semantics.
