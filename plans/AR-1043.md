# AR-1043: Qualify one exact topic-tip synchronization merge

## Outcome

Restore a green protected `main` after PR #132 preserved the exact reviewed tree and valid
GitHub-signed merge boundary but the protected-main policy rejected its signed topic-tip
synchronization merge. Support only that closed topology without weakening signature, DCO, tree,
or provenance requirements.

## Dependencies and ownership

Depends on AR-1040. Repository: `martin-beck/agent-systems-benchmark` only. Owned paths are
`tools/quality/repository_policy.py`, its focused tests and the minimum policy documentation and
attestation required to record the PR #132 recovery. Capability semantics, measurement contracts,
runner behavior, TUI, renderer, Ratatui and Crossterm code are out of scope.
Branch: `fix/protected-topic-sync-topology`. Worktree:
`agent-systems-benchmark-protected-topic-sync-topology`.

## Work sequence

1. Preserve PR #132 base `6155d63`, topic tip `297895d`, final merge `44eb1b48`, reviewed tree
   `1cde5c47`, valid GitHub verification, matching lowercase DCO trailer, 12/12 exact-head checks,
   and post-merge run 34548482976's exact topology failure.
2. Model the existing direct topic topology and one optional synchronization topology. The only
   optional topic merge must be the topic tip, have exactly two parents, use the protected range
   base as its second parent, and contain no other introduced merge commit.
3. Verify every pre-sync topic revision and the sync tip with the existing pinned SSH-signature and
   matching-DCO checks. Require the sync tip tree and final GitHub merge tree to be byte-identical,
   retain the final merge's exact first-parent, GitHub Web Flow identity/key and lowercase-DCO
   requirements, and reject unrelated commits in the protected range.
4. Add deterministic positive fixtures for the direct and exact sync forms and negative fixtures
   for off-tip, nested, octopus, multiple, arbitrary-base, unsigned, non-DCO, tree-changing and
   unrelated-range variants. Run the real PR #132 topology locally in protected-main mode.
5. Publish from a clean signed+DCO single-parent topic commit based on current protected main.
   Obtain immutable review and exact-head CI, merge with the repository's protected merge method,
   then verify exact tree, parents, GitHub signature, raw lowercase trailer and every post-merge
   `main` workflow. Do not rewrite or conceal the historical failed run.

## Acceptance criteria and evidence

Direct topics remain accepted; exactly one closed topic-tip sync merge is accepted only under all
listed identity, ancestry, signature, DCO and tree-equivalence constraints; every broader merge
shape is rejected; policy self-tests, full gates and coverage pass; the corrective PR has immutable
review and terminal-green exact-head checks; latest protected `main` is green while run
34548482976 remains truthfully recorded. No UI or product protocol behavior changes.
