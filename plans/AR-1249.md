# AR-1249: Complete MockAgents executable qualification evidence

## Outcome

Qualify the pinned MockAgents executable with the missing hostile lifecycle, network-denial,
repeat-clean-state, and emulated AArch64 evidence identified by AR-0896.

## Dependencies and ownership

Dependencies: AR-0888, AR-0889. Own only the ASB MockAgents qualification fixtures, evidence,
and applicable CI wiring; do not weaken existing gates or modify asb-tui.

## Acceptance

- Verify immutable executable/package/license identity and bounded launch provenance.
- Add credential-free positive and negative tests for hostile tool/tool-result handling,
  cancellation/backpressure, outbound-network denial, repeated clean-state runs, and the
  executable under emulated AArch64.
- Preserve fail-closed behavior and publish only sanitized bounded evidence.
- Run focused and full locked gates, independent review, signed-DCO exact-head CI, protected merge,
  post-merge verification, and release the AR only after all required checks pass.
