# AR-1298: Narrow task-metadata repair scope

Clean up AR-1297's over-broad normalization commit. Preserve only the strict
schema change and the metadata edits required by the validator failures that
AR-1297 recorded; restore unrelated task documents exactly to their pre-AR-1297
content.

## Scope and decision gate

Compare the signed pre-AR-1297 state with the current state and classify every
changed task. Retain only verified observed-head, bounded-next-action, and
superseded-chain repairs required for validation; restore all incidental
formatting or observation rewrites. Regenerate CURRENT/STATUS through handoffctl
and rerun the full quality/schema gates. Do not rewrite history, weaken schema,
or change product/asb-tui/handoffctl/formal behavior.

## Acceptance

- The diff is minimal and auditable: schema/test changes plus only the reported
  failing task repairs remain.
- `validate_schema`, render-status, full state tests/coverage, vendor/privacy,
  signatures/DCO, and live doctor pass.
- Every retained metadata field is supported by durable evidence and every
  unrelated task is byte-identical to the pre-AR-1297 state.
