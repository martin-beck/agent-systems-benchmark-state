# AR-0845 plan

Use quota-aware branching only for explicitly optional evidence. Keep required checks fail-closed,
record artifact absence as a classified result, and test zero-quota, delayed-recalculation, retry,
cancellation, and privacy paths.
