# AR-0846 plan

Adapt Agent Relay's linkedness/age/size/quota policy. Implement a deterministic dry-run inventory,
then an explicitly authorized bounded cleanup with immutable audit output and no blind retries.
