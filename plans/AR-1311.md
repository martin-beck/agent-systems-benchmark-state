# AR-1311: Planned-task metadata schema repair

## Objective

Repair the precise metadata contradiction found by the current task validator:
AR-1309 is planned and has no checkout, so its optional live-observation fields
must be absent rather than present with empty values. The task's empty
`checkpoint_commit` remains unchanged because no implementation checkpoint exists.

## Scope

Only `tasks/AR-1309-reviewed-capacity-or-model-reduction.md`, generated views, and
durable evidence for this repair. Do not change the task schema, AR-1309 plan or
meaning, product sources, handoffctl, formal gates, leases, or other task records.

## Acceptance

- Remove only empty unsupported `observed_branch`/`observed_head`/`observed_dirty`
  metadata from planned AR-1309; do not invent a commit or implementation result.
- `tests/validate_schema.py`, render-status, state tests, privacy, signatures/DCO,
  and live doctor pass, with generated views matching task records.
- The repair is SSH-signed, DCO-signed, independently reviewed, and leaves the
  formal qualification chain fail-closed.
