# AR-1006: Coordinate distributed experiment workers

## Outcome

Schedule trials across native-capability workers while preserving per-host capacity meaning.

## Dependencies and ownership

Dependencies: AR-0204, AR-0702, AR-1001.
Owned paths: distributed coordinator/worker protocol and integration tests.
Branch: feature/distributed-workers. Worktree: agent-systems-benchmark-distributed-workers.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Specify distributed control semantics after single-host measurement is stable.
2. Implement the smallest complete boundary and its independent failure tests.
3. Run focused and applicable full quality gates on the exact commit.
4. Publish a focused PR, reconcile CI and record verified limitations.

## Acceptance criteria

Capability advertisement, leases and fencing reject duplicate accepted completions; retry attempts remain distinct; worker loss/restart/partition/stale lease/duplicate artifacts pass; monotonic local durations and clock uncertainty prevent unsynchronized cross-host subtraction; host results stay separate.

## Evidence and completion

Record source/tool versions, commands by digest, immutable commit and CI identifiers,
observed outcomes and remaining limitations. No raw private configuration or transcripts.
Include negative cases and a real production-boundary integration where applicable.
Do not satisfy native evidence with mocks or emulation. Finish only after the requested
outcome, review, exact-head checks and post-merge checks pass.
