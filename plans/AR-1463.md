# AR-1463: Current-main first-customer requalification after capture integration

## Required work

1. Read all product development, architecture, quality, release and
   coordination documentation. Refresh refs and use a clean isolated worktree
   at exact protected main `01b70e87ce8e7913f614447c0c530cb22e235256` (or a
   later exact protected-main commit with a recorded checkpoint).
2. Verify install/bootstrap/configuration and the complete local/mock multi-agent
   workload path, including deterministic capture, redaction, content-addressed
   cassette sealing, strict offline replay, cancellation/restart and cleanup.
3. Run privacy/credential scans, network-denial and direct/alternate-egress
   negative tests, full workspace tests, coverage, format, Clippy, rustdoc,
   formal and pinned supply-chain gates, plus a release build and artifact
   reproducibility check.
4. Independently review the exact diff since AR-1461's release input, wait for
   exact-head hosted checks, and verify no asb-tui or remote-provider dependency
   was introduced. Do not publish a new tag or release unless the established
   release workflow says a versioned release is required and every gate passes.
5. Release this AR done only with exact current-main evidence and, if a release
   is appropriate, verify its tag, assets, checksums, SBOM/provenance and fresh
   downstream consumption. Otherwise record that the existing v0.1.0 remains
   the verified customer release and leave no publication side effect.

## Acceptance evidence

- exact protected main is clean and all first-customer local/mock journeys pass;
- capture/replay and privacy evidence covers AR-1330;
- full applicable deterministic and hosted gates are green;
- no live provider, native ARM host, asb-tui change or external signing
  authority is required;
- post-qualification state records the exact commit and release decision.
