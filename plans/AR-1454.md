# AR-1454: Protected-main tree-equality repair for runtime replay

## Objective

Repair the post-merge tree-equality failure after PR #328 without rewriting
protected history or weakening the repository policy.

## Required evidence

1. Preserve failed workflow `36188339692` and merge `452f3ca29390ab37cf3aff8c813b92b54b163b20`.
2. Build the repair branch from current `origin/main`, with a signed+DCO commit and no unrelated runtime change.
3. Verify exact-head required checks and independent review at the repair tip.
4. Verify the protected merge has exactly two parents, reviewed-topic tree equals merge tree, and equals deterministic merge preview.
5. Require all seven exact-main post-merge workflows to terminate successfully.
6. Reconcile state, update AR-1450, and release only with durable evidence.

## Boundaries

Do not force-push, rewrite `main`, suppress the policy failure, mark AR-1450
done before repair verification, or touch asb-tui. A live provider is not
required; use offline/container/VM evidence where applicable.
