# AR-1064: Repair tmux startup and server observation

## Outcome

Repair the first proven portability or readiness predicate causing the hosted `asb-tui` terminal
fixtures to stop at `tmux_startup_not_ready`, while preserving the authenticated server and process
authority model. The final exact-main Trusted run must reach the existing fixture assertions and
prove cleanup on both success and every failure path.

## Dependency and ownership

Dependency: AR-1062 (done). AR-1063 is the separately reserved publication successor and must
publish the diagnostic candidate and produce exact-main diagnostic evidence before this task is
promoted; it is deliberately not listed as a graph dependency until that task exists on state main.
Do not infer a repair from a generic local pass.
This task has no dependency on AR-1010 and must not create a reverse dependency on the foundation.

Repository: `martin-beck/asb-tui` only. Owned path is limited to the smallest existing terminal
foundation harness/source path required by the diagnostic result, normally
`tests/terminal_foundation.rs`; any expansion requires a new immutable review. No ASB source,
Ratatui/Crossterm integration, application rendering, lifecycle/router protocol, release metadata,
workflow, coordinator state, or UI feature work is permitted.

## Current failure evidence

Trusted-main workflow `34831918221` verified exact main `db612b6f` and failed after ordinary tests:

* `hostile_tmux_tmpdir_cannot_redirect_owned_server_or_cleanup`
* `local_tmux_and_screen_sessions_quit_and_restore_termios`
* `malformed_created_window_identity_drops_the_fully_authorized_guard`
* `tmux_guard_reaps_a_hup_resistant_owned_pane_group_after_readiness_failure`
* `tmux_guard_without_process_authority_still_cleans_its_exact_server`

The observed bounded diagnostics were `server_observation=socket_connect_rejected` for the local
fixture and `startup_observation=server_before_unavailable` for the remaining downstream fixtures.
The diagnostic output contains no private values, and the failure is not evidence that fixture
serialization, retries, or an authority predicate may be weakened.

## Required work sequence

1. Fetch and verify the exact AR-1063 merge and rerun its diagnostic tests on hosted Trusted main.
   Record the immutable head/tree, workflow run, toolchain, and complete failure classification.
2. Reproduce the same class repeatedly in serial and default-parallel modes. Capture only bounded
   symbolic stages and stable booleans; never record socket paths, PIDs, UIDs, errno numbers, raw
   command output, timing, credentials, or host-specific identifiers.
3. Map the first failing predicate through server-before, socket connect, server-after, pane/window
   identity, readiness, and cleanup. Add exhaustive injected tests before modifying the predicate.
   A test-only diagnostic change is a separate reviewable checkpoint if needed.
4. Repair only the proven predicate. Preserve owner/type/dev/inode socket bracketing, exact peer
   PID/start-time checks, repeated identity equality, pre-option/pre-signal revalidation,
   process-group ownership, exact server cleanup, and fail-closed behavior. Do not increase retry
   counts, delays, accepted error classes, authority scope, or cleanup reach without fresh evidence.
5. Run focused tests, repeated serial and parallel terminal suites, full locked tests/doctests,
   fmt, all-target Clippy, rustdoc, release, deny/audit, coverage, workflow/privacy/schema/
   publication/channel/isolation/self-test checks, signature/DCO, leak checks, and clean-tree
   checks under the pinned Rust 1.93.0 toolchain.
6. Obtain independent immutable review of the exact parent/tree/diff and push one signed DCO head.
   Require exact-head CI, protected merge verification, post-merge Repository quality, and a fresh
   Trusted-main run. Any red gate creates a separately scoped successor; it never justifies
   relaxing this task's acceptance criteria.

## Acceptance

* The exact hosted Trusted run reaches and passes all five affected fixtures repeatedly, in both
  serial and default-parallel modes, with no owned process, server, socket, pane, or terminal leak.
* The repair changes only the proven predicate and has an immutable review, signed DCO commit,
  exact-head green CI, protected merge, and exact-main evidence.
* Authentication, owner/resource identity, retry/timing policy, cleanup authority, success/failure
  decisions outside the proven predicate, and privacy boundaries remain fail-closed and unchanged.
* The diff contains no ASB product code, Ratatui/Crossterm rendering, standalone application UX,
  lifecycle installer/router, dependency, release, or workflow change.
