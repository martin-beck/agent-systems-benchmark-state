# AR-1064: Recover hosted tmux startup readiness

## Outcome

Make the existing authenticated tmux fixture acquire its server observation reliably on the hosted
trusted runner while retaining bounded, fail-closed behavior and all existing cleanup authority.

## Scope

`martin-beck/asb-tui`, one test-only file: `tests/terminal_foundation.rs`. No ASB code, renderer,
Ratatui application, lifecycle protocol, dependency, or workflow changes.

## Evidence and hypothesis

Trusted-main `34831918221` at `db612b6f07029b34caf7a80998b6cd1b10929567` fails five live fixtures
before identity/option work; local exact Rust 1.93.0 passes. A temporary local experiment using
500 bounded startup attempts passed all 31 terminal tests. Treat that only as a hypothesis: verify
hosted behavior and ensure total wait remains explicitly bounded.

## Implementation and tests

Use the smallest bounded readiness adjustment justified by the failure evidence. Keep all ownership,
peer authentication, socket identity, process-generation, pane/TTY, revalidation, and cleanup
predicates unchanged. Add focused tests for the bound and failure classification. Run focused,
serial, parallel, full locked, fmt, clippy, rustdoc, release, audit/deny, privacy, schema,
publication, signature/DCO, and clean-tree gates, then require exact-head CI and post-merge
Trusted-main.

## Acceptance

All five previously failing hosted fixtures pass, no live server/process leaks remain, and the diff
contains no UI or ASB implementation. Only then may AR-1010 be requalified.
