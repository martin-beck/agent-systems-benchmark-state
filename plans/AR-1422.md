# AR-1422: Stale agent-catalog PR cleanup

## Objective

Dispose of PR #306 safely after verifying that its conflicting branch is stale
and its intended agent-catalog implementation is already present on exact current
main through AR-1316's historical merges.

## Acceptance

- Verify PR #306 number, base/head/tree, conflict state, signatures, and changed
  paths against current `origin/main` and AR-1316 evidence.
- Confirm no unique unmerged product behavior remains; do not rebase, force-push,
  merge, or delete evidence.
- Add a concise public comment identifying the superseding current-main revision
  and close the PR as superseded through the established handoffctl workflow.
- Record the exact comment/closure result in this AR, reconcile state, and run the
  live doctor. If any unique delta is found, leave the PR open and create a scoped
  successor instead of closing it.

## Non-goals

No product edits, no asb-tui changes, no gate changes, and no live-provider access.

