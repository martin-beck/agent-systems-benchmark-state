# AR-0854: Upgrade the shared coordinator to v0.2.0

## Scope

- Vendor the immutable signed v0.2.0 release.
- Preserve ASB-specific configuration, schemas, and extensions.
- Add the new formal run model to downstream verification.
- Keep expensive coordinator CI path-scoped to coordinator files.

## Acceptance

- Offline vendor verification matches the v0.2.0 tag and commit.
- Strict typing, lint, schema, branch coverage, and all coordinator tests pass.
- Every vendored TLA+ safety and liveness model passes.
- A reviewed signed+DCO PR is merged and post-merge checks are green.
- The live checkout is clean, synchronized, and passes live doctor.
