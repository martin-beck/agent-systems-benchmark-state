# AR-0308: Implement mini-SWE-agent client adapter

## Outcome

Run a pinned MIT mini-SWE-agent release as a minimal batch software-engineering agent.

## Dependencies and ownership

Dependencies: AR-0101, AR-0102, AR-0103.
Owned paths: asb-agents mini-SWE-agent module and its fixtures.
Branch: feature/agent-mini-swe. Worktree: agent-systems-benchmark-agent-mini-swe.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Inspect exact SWE-agent/mini-swe-agent source, MIT license, release/package metadata, CLI trajectory format, LiteLLM endpoint controls and environment backends.
2. Pin the Python dependency graph and source/package integrity; isolate configuration, dotenv, histories and environment selection.
3. Exercise a real local or Bubblewrap-backed batch agent against the common credential-free loopback fixture, including edits, commands, budgets, cancellation and malformed trajectories.
4. Run focused and full gates, publish a focused PR, reconcile exact-head CI and record limitations.

## Acceptance criteria

A real pinned package completes the common offline fixture and produces bounded causal lifecycle/tool/usage evidence. Model/base-URL override, dependency integrity, config isolation, action budget, cancellation and descendant cleanup are proven. Benchmark claims from upstream README material are not treated as ASB evidence.

## Evidence and completion

Record exact source/tag/tree, license, Python lock and package integrity, commands by digest, immutable commit, CI identifiers, native outcomes and unsupported combinations. No credentials, raw trajectories, prompts, transcripts or private paths. Finish only after independent immutable-head review, exact-head checks, integration and post-merge verification.

