# AR-1333: Multi-agent by workload benchmark campaign

## Outcome

A benchmark campaign mode drives the full agent-by-workload matrix under one
pinned OpenRouter free-model selection: each tuple executes, captures, seals,
and records its cassette, and the whole matrix replays strictly offline for
reproducible comparison. Intent persists before provider effects, interrupted
tuples reconcile to stale without repeating paid work, and aggregate counts
cannot satisfy the coverage gate.

## Dependencies and ownership

Dependencies: AR-1329 (live run execution) and AR-1332 (record-live to
replay-offline workflow). Owned paths: the campaign command and matrix
expansion, per-tuple execution/capture/seal/record, interruption
reconciliation, and offline replay of the sealed matrix in the ASB product.
Branch: `feature/ar-1333-multi-agent-workload-campaign`. Worktree:
`agent-systems-benchmark-ar-1333-multi-agent-workload-campaign`.

## Work sequence

1. Define the campaign command that expands the compatible agent matrix over
   the in-tree workload suite under one pinned free-model selection.
2. For every tuple, persist intent, execute under explicit opt-in, capture and
   seal the cassette, and record agent, workload, model, digest, result, and
   replay outcome.
3. Make execute/progress/reconcile/cancel idempotent and restart-safe with
   exact provider, model, workload, and runtime identity matching.
4. Support offline replay of the complete sealed matrix; reject missing,
   duplicate, stale, or mismatched tuples.
5. Add campaign tests: full matrix, partial/interrupted campaign, idempotent
   restart, privacy scanning, and offline replay of the sealed matrix.

## Acceptance criteria

The campaign produces a per-tuple, digest-anchored evidence matrix for every
selected agent and workload; interrupted tuples never repeat uncertain paid
work; offline replay reproduces the matrix without provider contact; and no
aggregate summary can stand in for complete per-tuple coverage.

## Evidence and completion

Record immutable commits, matrix fixtures, per-tuple evidence, interruption and
replay results, independent review, exact-head CI, and post-merge verification.

## Current development and CI qualification boundary

The mandatory development and CI qualification path for this AR is a deterministic
local provider/LLM mock (LiteLLM-compatible where practical), including hostile
negative tests and offline replay where applicable. External/live OpenRouter or
other provider reachability is optional supplementary evidence only; it is never a
completion, dependency-readiness, or CI gate. Production egress policy, credential
non-disclosure, runtime-owned authority, namespace/relay attestation, cancellation
and teardown, and fail-closed denial of unapproved external traffic remain required
contracts. Existing live-provider dependency edges describe production integration
ordering only and must not be used to block local qualification or to claim external
reachability.
