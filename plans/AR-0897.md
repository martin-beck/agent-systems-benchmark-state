# AR-0897: Restore main merge integrity

## Outcome

Restore a green, auditable exact-main commit-policy boundary without rewriting published history, and prevent GitHub-generated merge commits from bypassing the required SSH-signature and DCO integration process.

## Dependencies and ownership

Dependencies: AR-0003.
Owned paths: merge/integration tooling, repository settings, contribution and development documentation, commit-policy fixtures, and repair evidence.
Branch: `fix/main-merge-integrity`. Worktree: `agent-systems-benchmark-main-merge-integrity`.

## Work sequence

1. Preserve published commit `b6d04a8305ce6d49cc327e4e6d2d6fa42a88050b`; record its exact tree, parents, failed run 34347816992, and the fact that later quality steps were skipped.
2. Add a signed DCO-bearing descendant repair/attestation commit and obtain a successful exact-main run that scans the repair range without claiming the historical merge became signed.
3. Provide one reviewed integration command that fetches an immutable approved PR head, creates a local `--no-ff --gpg-sign --signoff` merge, verifies tree/parents/signature/DCO, and pushes only by exact lease.
4. Disable incompatible GitHub web merge modes or add an equivalent fail-closed repository control; document the authorized integration path and recovery from rejected or ambiguous pushes.
5. Add fixtures proving unsigned, non-DCO, wrong-head, wrong-tree, stale-base, and GitHub-generated merge commits are rejected before publication.

## Acceptance criteria

Current main has fresh green exact-main evidence; published history is not rewritten; the historical limitation is explicit; future integrations cannot use a merge path that creates an unsigned or non-DCO commit; reviewed PR tree identity is preserved; and ambiguous publication is reconciled before retry.

## Upstream boundary

The shared coordinator wraps authorized commands but does not create ASB product merge commits. Escalate upstream only if implementation proves a missing generic coordinator primitive rather than an ASB repository-policy problem.

## Evidence and completion

Record immutable refs and trees, signature/DCO verification, repository-setting evidence, negative fixtures, exact-head and post-merge CI, and live coordination reconciliation.
