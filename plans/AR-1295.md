# AR-1295: Align coordinator vendor fixtures with the immutable v0.3.7 pin

Update the state repository's vendor-verification fixtures after AR-1294
restored the checked-in payload to the immutable coordinator v0.3.7 release.
The current tests still assert the superseded v0.3.5 manifest and digest, so
they fail before any formal runner behavior is exercised.

## Scope and decision gate

Use only the already verified upstream identity
`550c014c440cc9bc45727fea71d90a9025c554c3` and the checked-in v0.3.7 payload.
Update expected versions, digest fixtures, and negative cases mechanically while
preserving the fail-closed behavior for wrong versions, commits, files, and
runtime identities. Do not change vendor code, handoffctl, product sources, or
formal admission semantics.

## Acceptance

- The full vendor test suite passes against the exact v0.3.7 manifest and
  checked-in payload, including all negative cases.
- `handoffctl_vendor.py verify`, privacy, signature/DCO, and full state gates
  pass offline; no upstream download or mutable tag is trusted.
- Any remaining TLC host-capacity failure is recorded separately and does not
  get hidden or classified as a product success.
