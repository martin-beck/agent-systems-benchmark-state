# AR-1421: Protected-main literature merge race repair

## Objective

Repair the integration/requalification path exposed when PR #307 was reviewed
against `0dcc717`, PR #305 advanced main to `a2a6414`, and protected-main
Repository Quality run `36048870322` rejected merge `5ddac12f` because the
protected merge tree differed from the reviewed topic tree.

## Dependencies and scope

- Depends on AR-1417 (preserve its product merge and failure evidence) and AR-1398
  (signed protected-main recovery baseline).
- Coordinator/integration boundary only; no workload semantics, OpenRouter behavior,
  native-evidence weakening, or `asb-tui` changes.
- All product/Git/test/review/publication mutations use the state repository's
  `handoffctl run` boundary and isolated `/srv/data/projects` worktrees.

## Acceptance

1. Reproduce the failure with the exact base, topic, merge, and workflow inputs;
   distinguish target advancement/tree mismatch from a product test failure and
   retain the original run as immutable evidence.
2. Implement the smallest reviewed repair so merge admission revalidates target
   head, topic head, reviewed tree, parent topology, and required checks immediately
   before publication, or schedules a fresh exact-main qualification whenever the
   target advances. No commit-specific allowlist or gate suppression is allowed.
3. Add positive and hostile tests for concurrent main advancement, stale topic
   trees, exact-tree mismatch, cancelled post-merge runs, and a successful serialized
   merge/requalification path.
4. Run focused/full gates, signature/DCO/privacy checks, exact-head CI, independent
   review, and all seven terminal-success post-merge workflows for the fresh repair.
   Keep AR-1417 open until its failed run is replaced by truthful fresh evidence.

## Non-goals

Do not alter the workload implementation, OpenRouter/provider contracts, benchmark
scores, native platform policy, or release gates.

