# AR-1063: Publish and qualify nested tmux diagnostics

## Outcome

Publish the exact diagnostic behavior developed under AR-1062 as one newly SSH-signed,
DCO-trailed commit in `martin-beck/asb-tui`, obtain independent immutable review and exact-head CI,
and use merged exact-main Trusted evidence to expose the hidden nested tmux server/connect failure.
Do not change success decisions, authority boundaries, retry policy, application behavior, or UI.

## Dependency and ownership

Dependency: AR-1062. AR-1062 produced a locally tested diagnostic candidate but publication and
exact-main qualification did not occur before its terminal transition. The coordinator has no
supported `done`-to-`open` transition, so this successor owns only the missing signed publication
and qualification stages.

Repository: `martin-beck/asb-tui` only. Owned path: `tests/terminal_foundation.rs`. Branch:
`fix/tmux-nested-server-connect-diagnostics-signed`. Worktree:
`agent-systems-benchmark-asb-tui-tmux-nested-diagnostics-publication`. No ASB source, renderer,
application, protocol, dependency, workflow, release-channel, lifecycle, authentication, retry,
timing, cleanup, or success-decision path is owned.

AR-1010 must remain open until this successor is done with green exact-head Repository quality and
exact-main Trusted-main evidence. Its dependency graph may be amended only through a separately
reviewed state change; this task does not bypass the current superseded prerequisites.

## Immutable evidence

- Protected `asb-tui` main is `db612b6f07029b34caf7a80998b6cd1b10929567`, tree
  `e4777aef699520acd74780a215ba85963f980604`.
- Unsigned AR-1062 source commit `defffe34caf27ac293dee34d12ada5c30a3aea17` has parent the exact
  main above and tree `0a84455ca544a68999721e4744dc8c885071154d`.
- Signed+DCO attestation `6d0fe4b986f23077066c87b5bf019fac5d889c63` has the same tree but
  parent `defffe34caf27ac293dee34d12ada5c30a3aea17`; it proves tree identity but is not publishable
  because its range includes the unsigned source commit.
- Stable patch ID is `a86107181814c1dc699e7eb0a1a4814e41fd2986`.
- Exact scope is `tests/terminal_foundation.rs`, 252 insertions and 73 deletions.

## Work sequence

1. Fetch `asb-tui` and prove remote main still equals the immutable base. If main moved, stop and
   record the new parent/tree and obtain a fresh review; patch-ID equality alone is insufficient.
2. Recreate the source diff as one new SSH-signed+DCO commit directly parented on protected main.
   Do not publish `defffe3`, `6d0fe4b`, or any range containing the unsigned commit.
3. Prove exact parent, tree, stable patch ID, changed-path set, numstat, and full-diff equality. Any
   difference requires a new immutable review and updated state evidence.
4. Run Rust 1.93.0 focused classifier/propagation tests, repeated serial and default-parallel
   terminal suites, full locked tests/doctests, format, all-target Clippy, rustdoc, release, deny,
   audit, coverage, workflow/privacy/publication/channel/isolation/self-test, signature, DCO, leak,
   and clean-tree gates.
5. Obtain independent immutable approval, push only the signed head, and open a focused PR. Require
   every exact-head check to finish green before protected merge.
6. Verify the merge tree, signature, and raw DCO independently, then require green exact-main
   Repository quality and Trusted main.
7. Read only the fixed privacy-safe nested context and symbolic error class. If it identifies a
   behavioral defect, create a separate bounded repair AR; do not add behavior here.

## Acceptance

- The published commit is signed, DCO-trailed, directly parented on the protected main base, and
  exactly reproduces tree `0a84455ca544a68999721e4744dc8c885071154d` and patch ID
  `a86107181814c1dc699e7eb0a1a4814e41fd2986`.
- The PR, exact-head checks, protected merge, and exact-main workflows refer to the independently
  reviewed tree; Repository quality is green.
- Trusted main reports only fixed bounded nested context and a symbolic connection class, with no
  errno number, socket path, PID, UID, raw output, timing, credential, or host information.
- No authentication, retry, timing, cleanup, success decision, renderer, application, protocol,
  dependency, release, or ASB path changes.
- No owned process, tmux server, socket, pane, or terminal state leaks.
- AR-1063 remains nonterminal until merge provenance and both exact-main workflows are verified. A
  red Trusted run creates a separate repair AR and never permits weakening this diagnostic task.

## Evidence to record

Record the signed candidate parent/tree/patch ID, scoped diff and numstat, toolchain and gate
results, independent reviewer approval, PR and exact-head run identities, merge tree/signature/DCO,
exact-main run identities, bounded diagnostic outcome, leak audit, and any successor repair AR.
