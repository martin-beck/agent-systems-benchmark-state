# AR-1039: Attest the measurement catalog merge boundary

## Outcome

Restore a green protected `main` after GitHub's PR #131 merge commit preserved the reviewed tree and
valid GitHub signature but omitted a matching DCO trailer.

## Dependencies and ownership

No implementation dependency. This is a focused recovery for AR-1013 in
`martin-beck/agent-systems-benchmark` only. Owned paths are one bounded machine-readable merge
attestation, its validation test or catalog registration, and the minimum documentation needed to
record the immutable publication boundary. No catalog semantics, runner behavior, TUI, renderer,
Ratatui, Crossterm, screen, widget or frontend state is owned.
Branch: `docs/measurement-catalog-merge-attestation`. Worktree:
`agent-systems-benchmark-measurement-catalog-merge-attestation`.

## Work sequence

1. Record PR #131's reviewed base/head/tree, all 12 successful exact-head checks, merge commit and
   GitHub signature verification, and the precise missing-DCO classification without rewriting
   protected history or claiming the merge commit itself was compliant.
2. Add deterministic validation that binds the attestation to the existing catalog fixture or
   contract catalog and rejects unknown fields, malformed hashes, false signature/DCO claims and
   mismatched reviewed/published trees.
3. Publish the corrective pull request from a signed+DCO commit. Merge only with `--merge` and an
   actual multiline merge body ending in the matching Martin Beck `Signed-off-by` trailer, so the
   new GitHub-signed merge commit itself passes repository policy.
4. Verify both parent identities, reviewed and published tree equivalence, GitHub verification,
   raw trailers, exact-head CI and all post-merge `main` workflows.

## Acceptance criteria

The immutable non-DCO merge remains visible and accurately attested; the corrective reviewed commit
and its GitHub merge commit are both signed and DCO-compliant; current `main` required checks are
green; the catalog product tree is unchanged except for bounded attestation/documentation/testing;
and AR-1013 can cite this recovery before release.

## Evidence

Record exact commit/tree/parent hashes, GitHub verification JSON classification, 12/12 original
head checks, attestation validation negatives, full applicable quality/privacy gates, independent
review, corrective exact-head CI and post-merge main verification.
