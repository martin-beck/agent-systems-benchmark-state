# AR-0835 plan

Use the audit evidence as the baseline. Inspect workflow output and GitHub runner behavior, then
either suppress identity-bearing steps or narrow claims to acknowledge unavoidable platform output.
Enforce repository/ref guards consistently and prove the boundary with negative fixtures and
independent privacy review before any trusted workflow is dispatched.
