# AR-1313: Historical DCO merge-integrity recovery

## Scope

Create the smallest forward-only signed/DCO recovery for historical merge
`909078ced21f36e5a72590c9decf41ac56452212`. Preserve the old object and exact product tree;
do not rewrite published history, forge a trailer, or weaken repository policy. Use a clean
worktree and the existing signed recovery/merge workflow.

## Acceptance

- Exact historical defect and preserved ancestry are recorded.
- Candidate commits are SSH-signed with matching DCO and independently reviewed.
- Required exact-head CI and post-merge main policy/coverage gates pass.
- If the current policy cannot accept a forward-only recovery, record the precise blocker and
  leave the AR blocked rather than bypassing the gate.
