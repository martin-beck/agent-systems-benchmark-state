# AR-1061: Isolate concurrent trusted tmux fixtures

## Outcome

Determine whether the remaining exact-main tmux failures are caused by the five live tmux fixtures
running concurrently, then serialize only those test fixtures if the evidence confirms contention.
Preserve every authenticated server, socket, pane, TTY, process-generation and cleanup invariant;
change no product, UI, renderer or lifecycle behavior.

## Scope and ownership

Recovery parent: AR-1058. Exact asb-tui base:
`69fecc01a56f5e9a4b641b4e604e9289249c5c76`, tree
`8df14c92dec015839c67154f00af8dd4a659c8d4`. Repository: `martin-beck/asb-tui` only. Owned path:
`tests/terminal_foundation.rs`. Branch: `fix/tmux-live-fixture-isolation`. Worktree:
`agent-systems-benchmark-asb-tui-tmux-live-fixture-isolation`. No application, renderer,
lifecycle protocol, dependency, release, workflow or ASB source change.

AR-1058 remains open pending this recovery. AR-1010 is explicitly gated on AR-1061 and green
exact-main qualification; AR-1061 has no dependency on AR-1010, so the graph remains acyclic.

## Exact failure evidence

AR-1058 PR #22 exact-head Repository quality run `34565747286` passed. GitHub-verified squash
`69fecc01a56f5e9a4b641b4e604e9289249c5c76` has parent `37613e81`, exact reviewed tree
`8df14c92`, and a valid raw multiline DCO trailer. Postmerge Repository quality run `34565894817`
passed. Trusted-main run `34565894753` failed 5 of 30 terminal tests: the primary interactive
fixture reported `server_observation=socket_connect_rejected`; the HUP-resistant and malformed-
window fixtures reported `startup_observation=server_before_unavailable`; the hostile-TMUX_TMPDIR
and no-process-authority failures were downstream. All five live tmux fixtures execute in the same
default-parallel Rust test binary, while the unchanged serial suite passes locally. This localizes
the next bounded experiment to fixture scheduling rather than weaker authentication.

## Implementation and proof

1. Add one process-local test mutex covering the complete lifetime of each live tmux fixture,
   including server creation, observation, option setup, interaction and authenticated cleanup.
   Pure parser/classifier tests remain parallel.
2. For the hostile-environment subprocess fixture, acquire the lock in the parent before spawning
   the exact child and let the child execute under that retained parent exclusion; never reacquire
   recursively in the child.
3. Add deterministic two-way synchronization proving a second live fixture cannot enter before the
   first releases the lock. Recover poisoning only to permit authenticated RAII cleanup after an
   earlier assertion; never use the mutex as cleanup authority.
4. Retain unique owner-private socket/session roots and every exact identity check. Do not broaden
   retries, accepted socket errors, credentials, process identities, signals or server teardown.
5. Run repeated default-parallel and serial terminal suites with scoped leak evidence. Merge only
   after immutable review and exact-head CI; exact-main Trusted verification decides acceptance.

## Tests and gates

Run focused synchronization and all five live fixtures, repeated serial and default-parallel
terminal suites, full locked tests, format, all-target Clippy, rustdoc, release, deny/audit,
coverage, shell/workflow/privacy/signature/DCO and clean-tree checks. Final acceptance requires
exact-main Repository quality and Trusted main verification green with no owned process or live
tmux server leak.

## Acceptance

All five live tmux fixtures pass in the default-parallel trusted suite while preserving their exact
authenticated cleanup boundaries. The diff remains one test-only file and contains no product,
UI, renderer or ASB implementation.
