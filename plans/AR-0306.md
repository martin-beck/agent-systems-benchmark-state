# AR-0306: Implement Qwen Code client adapter

## Outcome

Run a pinned Apache-2.0 Qwen Code release through headless stream-JSON.

## Dependencies and ownership

Dependencies: AR-0101, AR-0102, AR-0103.
Owned paths: asb-agents Qwen Code module and its fixtures.
Branch: feature/agent-qwen-code. Worktree: agent-systems-benchmark-agent-qwen-code.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Inspect exact QwenLM/qwen-code source, license, stable npm/container artifacts, stream-JSON schema, provider endpoint controls, context discovery and permissions.
2. Pin content and isolate HOME/XDG/project configuration so QWEN.md, sessions, skills, telemetry and ambient credentials cannot change the attempt.
3. Exercise real completion, edits, tools, cancellation, partial events and malformed/oversized output against a credential-free loopback service.
4. Run focused and full gates, publish a focused PR, reconcile exact-head CI and record limitations.

## Acceptance criteria

A real pinned CLI completes the common offline fixture and maps bounded causal lifecycle/tool/usage events; provider override and config isolation are proven. Buffered and streaming modes, cancellation, turn/action ceilings, package architecture evidence and every unsupported feature are explicit.

## Evidence and completion

Record exact source/tag/tree, license and package/container integrity, commands by digest, immutable commit, CI identifiers, native outcomes and unsupported combinations. No credentials, raw prompts, transcripts or private paths. Finish only after independent immutable-head review, exact-head checks, integration and post-merge verification.

