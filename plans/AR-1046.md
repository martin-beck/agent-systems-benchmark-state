# AR-1046: Serialize the emulated AArch64 agent fixture lane

## Outcome

Restore deterministic emulated AArch64 qualification after two consecutive protected-main/PR
runs failed different Gemini fake-node readiness tests with `HookUnavailable`. Keep production
runtime semantics and the complete test inventory unchanged; remove only test-harness scheduling
contention inside the existing QEMU `asb-agents` library-test invocation.

## Failure evidence

- Current-main run `34550483005` failed
  `gemini::tests::duplicate_and_stale_route_ownership_fail_closed_then_cleanup_allows_reuse` with
  `HookUnavailable`; 138 tests passed, one failed, one was ignored, and one existing test was
  filtered by the workflow's pre-existing explicit skip.
- PR #137 exact-head run `34550643921`, job `103112635225`, failed
  `gemini::tests::ambient_config_prompt_limits_and_cancellation_fail_closed` with the same
  `HookUnavailable`; the same 138/1/1/1 aggregate proves the lane reached a different readiness
  fixture before failing.
- Both failures occurred under the existing QEMU AArch64 library-test command. The different tests
  and shared readiness error identify concurrent fake-process starvation/contamination rather than
  an AR-1043 protected-policy semantic failure or an unavailable hosted runner.

## Dependencies and ownership

No implementation dependency. Repository: `martin-beck/agent-systems-benchmark` only. Owned paths
are `.github/workflows/emulated-aarch64.yml` and
`tests/platforms/test_emulated_aarch64.py`. No Rust production source, Gemini adapter behavior,
protocol, benchmark runner, frontend, renderer, Ratatui, or Crossterm changes are in scope.
Branch: `test/serialize-emulated-aarch64-agents`. Worktree:
`agent-systems-benchmark-emulated-aarch64-agent-serialization`.

## Work sequence

1. Preserve the existing immutable AArch64 userspace/image, cross-build, offline execution,
   timeout, and the single pre-existing mini-swe skip.
2. Add only `--test-threads=1` to the existing `cargo +1.93.0 test --offline --locked --target
   aarch64-unknown-linux-gnu -p asb-agents --lib -- ...` invocation. Do not add filters, split the
   inventory, retry failures, or change production timeout/readiness semantics.
3. Extend the existing platform workflow contract test to assert exactly one serialized
   `asb-agents --lib` invocation, the unchanged skip, and absence of extra filters/retries/splits;
   retain the existing command and time budgets.
4. Run the focused platform contract tests, workflow/static quality checks, and applicable full
   repository gates. Publish a signed+DCO single-parent commit from exact current main, obtain
   immutable review, and require all 12 exact-head checks including AArch64 to pass.
5. Merge only through the protected PR path with a genuine multiline body ending exactly
   `Signed-off-by: martin-beck <martin.beck2@gmx.de>`. Verify parents, exact reviewed tree, pinned
   GitHub signature, raw trailer, and every exact-main post-merge check before releasing this AR.
6. Rebase AR-1043's preserved exact patch cleanly onto the recovered main; do not rerun or merge
   stale PR #137 head `c76975e8c0b986fc9dd5b013467021f054182981`.

## Acceptance criteria and evidence

The AArch64 workflow runs every `asb-agents` library test exactly once and serially, preserving its
single existing skip and all build/runtime budgets. Both prior failures remain durably classified.
Focused and full local gates, independent immutable review, all 12 exact-head checks, exact merge
identity/signature/DCO, and every post-merge main workflow are terminal green. The diff is exactly
the workflow and its contract test; no production or TUI code changes.
