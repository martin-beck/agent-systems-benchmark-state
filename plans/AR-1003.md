# AR-1003: Enforce cost token and action budgets

## Outcome

Bound and report wall time, actions, tokens and monetary cost without treating unavailable telemetry as zero.

## Dependencies and ownership

Dependencies: AR-0101, AR-0301, AR-0302, AR-0303, AR-0304.
Owned paths: asb-core budgets, adapter accounting and analysis reports.
Branch: feature/execution-budgets. Worktree: agent-systems-benchmark-execution-budgets.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Specify budget capabilities and normalize provider usage with explicit uncertainty.
2. Implement the smallest complete boundary and its independent failure tests.
3. Run focused and applicable full quality gates on the exact commit.
4. Publish a focused PR, reconcile CI and record verified limitations.

## Acceptance criteria

Reserve enforceable budgets before calls; report measured versus estimated usage, price-table revision and cache/reasoning token classes; opaque adapters declare enforcement gaps; compute cost per success and quality-latency-resource Pareto sets against reference vectors.

## Evidence and completion

Record source/tool versions, commands by digest, immutable commit and CI identifiers,
observed outcomes and remaining limitations. No raw private configuration or transcripts.
Include negative cases and a real production-boundary integration where applicable.
Do not satisfy native evidence with mocks or emulation. Finish only after the requested
outcome, review, exact-head checks and post-merge checks pass.
