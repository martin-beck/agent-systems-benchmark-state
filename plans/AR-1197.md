# AR-1197: Qualify control scratch-root isolation on repaired ASB main

Acceptance criteria:

- PR #173 is reviewed against exact base `4f855514c5086e1a933ba1e5a4f41db135dbf0a8` and exact
  head `39f90b316faf4bbbce4170d7f0289c99355c9642`;
- concurrent control invocations receive disjoint private scratch roots and cleanup cannot remove
  another invocation's root;
- the tests cover concurrent creation, reopen-after-cleanup and hostile path/ownership cases, and
  remain renderer-neutral and outside the standalone asb-tui application;
- all required exact-head CI checks are terminal green, an independent immutable-head review is
  recorded, and the protected merge plus post-merge assurance are verified;
- any lifecycle PR rebase is checked against the resulting main tree so PR #173 cannot silently
  invalidate ASB v1.4/v1.5 protocol fixtures or asb-tui integration.
