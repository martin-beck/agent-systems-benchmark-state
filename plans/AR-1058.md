# AR-1058: Diagnose trusted tmux startup observation

## Outcome

Identify and repair the exact session, window or pane-process observation that still fails all five
trusted-main tmux fixtures after authenticated server acquisition became portable. Preserve the now
proven server socket/peer authority and every cleanup invariant; change no product UI/runtime code.

## Scope and ownership

Recovery parent: AR-1056. Exact asb-tui base:
`37613e8128ac6ee2a6bc692fb8dea6bc031ba949`, tree
`dfb41570127d45f59ecd0767af3487169e9d5ae8`. Repository: `martin-beck/asb-tui` only. Owned path:
`tests/terminal_foundation.rs`. Branch: `fix/tmux-startup-observation-diagnostics`. Worktree:
`agent-systems-benchmark-asb-tui-tmux-startup-observation-diagnostics`. No renderer, application,
lifecycle protocol, dependency, release, workflow or ASB source change.

AR-1056 remains open pending this recovery rather than becoming a reverse dependency. AR-1010 is
explicitly gated on AR-1058 and exact-main qualification; AR-1058 has no dependency on AR-1010, so
the graph remains acyclic.

## Exact failure evidence

AR-1056 PR #21 exact-head Repository quality `34564281649` passed. GitHub-verified squash
`37613e8128ac6ee2a6bc692fb8dea6bc031ba949` has parent `092cf20a`, the exact reviewed tree, and a
valid raw multiline DCO trailer. Postmerge Repository quality `34564426536` passed. Trusted-main
run `34564426538` failed 24 of 29 terminal tests: all five live tmux fixtures now report only generic
`tmux_startup_not_ready`, with no `server_observation=` suffix. This proves authenticated server
socket acquisition succeeds and localizes the remaining defect to the later stable startup
observation: session identity, window identity, pane identity, TTY identity, process table tuple,
process generations, or before/after stability.

## Diagnostic and repair sequence

1. Refactor `tmux_startup_observation` and its pane-process observation behind a closed diagnostic
   result without changing success behavior. Stages must distinguish only fixed categories:
   server/session/window before unavailable; pane identity unavailable or malformed; TTY identity
   unavailable; pane generation unavailable; process list before/after unavailable; process tuple
   invalid or changed; foreground generation unavailable; pane/TTY/pane-generation/foreground-
   generation changed; session/window/server after unavailable; and final server/session/window
   transition.
2. Emit only the final fixed stage when bounded startup acquisition exhausts. Never emit PID, UID,
   TTY, path, session/window identity, process output, timing or raw command errors. Labels are
   unique bounded lowercase ASCII.
3. Add injected stage/transition fixtures for every branch and retain all parser, substitution,
   generation, TTY, signal-recorder, malformed-window, HUP-resistant and leak tests.
4. Merge only the approved diagnostic checkpoint and use exact trusted-main evidence to select a
   repair. Do not weaken server, pane, TTY, process-generation or signal authority speculatively.
5. Repair only the proven unavailable predicate with an equally strong bounded observation. Missing
   or changed authority must still perform zero option, signal, server kill or unlink.

## Tests and gates

Run repeated serial and parallel terminal suites with scoped process/server leak checks, format,
Clippy, locked tests, rustdoc, release, coverage, cargo-deny/audit, schemas, channel/publication,
promoted self-test, shell/workflow, privacy, signature/DCO and clean tree. Each checkpoint requires
fresh immutable review, exact-head CI and separately authorized protected integration. Final
acceptance requires both exact-main Repository quality and Trusted main verification green.

## Acceptance

Trusted evidence identifies one closed startup/pane substage; the exact repair then makes all five
live tmux fixtures pass without weakening authority. No owned process or live server leaks, all
gates are green, and the final diff remains one test-only file with no product UI/render work.
