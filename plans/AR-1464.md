# AR-1464: Formal capacity and signed-input provisioning repair

## Required work

1. Read the complete AR-1307, AR-1308 and AR-1293 plans plus all state
   development and formal-runner documentation. Reconcile the exact remote
   AR-1307 head and signed/DCO history; do not silently substitute stale
   `969eef058` metadata for the authoritative reviewed head.
2. Assemble an immutable source bundle for the exact reviewed AR-1307 head,
   including tree/payload digest, signature/DCO evidence, model/config digests,
   canonical admission lock and deterministic guest seed. Reject mismatches,
   unsigned or synthetic development inputs.
3. Provision the reviewed Ubuntu x86_64 guest image and a 64 GiB overlay/data
   volume under `/srv/data/projects`. Verify QEMU architecture, image digest,
   8 vCPU, 32 GiB guest RAM, 16 GiB guest swap, no network, no host mounts and
   the unchanged 3G/3G/2-worker/2-core/8G/7200s process contract.
4. Provide at least 1 GiB available host swap without touching unrelated
   services or broad host state. Prefer a bounded, documented runner-local
   arrangement; if host provisioning cannot be performed safely, record the
   exact authority/capacity blocker and leave the AR blocked.
5. Run only bounded signed preflight checks. Do not boot QEMU/TLC until every
   input and capacity gate passes. Emit only sanitized receipts and digests;
   never retain raw model output, credentials, private paths or unbounded logs.
6. Run focused/full applicable state tests, privacy/provenance checks and
   independent review. Merge only through the exact state PR workflow; then
   update AR-1308 with immutable input paths/digests and the next canonical
   qualification command. This AR itself does not claim formal qualification.

## Acceptance evidence

- exact signed AR-1307 source/tree, seed, lock, model/config, JDK/TLC and image
  digests are present and cross-validated;
- capacity receipt proves 64 GiB data, 32 GiB RAM, 16 GiB guest swap, no
  network/mounts and at least 1 GiB available host swap;
- signed preflight passes or a precise external capacity blocker is recorded;
- no formal limit, gate, product source or asb-tui boundary was weakened.
