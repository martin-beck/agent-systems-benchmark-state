# AR-1315: Formal runner development-profile metadata repair

Update only the state task/plan and validator-facing metadata needed to distinguish
`diagnostic-only` unsigned development fixtures from signed formal qualification. Preserve all
resource, admission, provenance, attestation, exact-head, and release gates. Add schema-valid
positive/negative examples and reconcile generated views.
