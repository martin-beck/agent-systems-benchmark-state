# AR-1282: Land authenticated replay transport foundation

## Outcome

Land a narrowly scoped authenticated, bounded runtime-to-CLI replay transport foundation on
protected main so later ARs can safely integrate primary dispatch without fabricating authority.

## Dependencies and ownership

Dependencies: AR-1237, AR-1238, AR-1239. Prior AR-1275 through AR-1281 are blocked evidence only;
do not reuse their branches. Owned paths: asb-core transport envelopes, asb-runtime one-shot
runtime issuer/client, unit and negative tests, and generated provenance fixtures. Branch:
`feature/ar-1282-authenticated-replay-transport`; worktree:
`agent-systems-benchmark-ar-1282-authenticated-replay-transport`.

## Acceptance

The merged foundation provides bounded request/response envelopes, runtime-owned one-shot client
issuance, identity and schema validation, duplicate/closed/fallback rejection, and signed/DCO
evidence. It must not claim primary CLI execution or lifecycle qualification. Run focused and full
locked gates, independent review, exact-head CI, signed merge, and post-merge assurance.
