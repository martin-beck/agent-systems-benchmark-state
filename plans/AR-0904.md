# AR-0904: Machine-check protocol and artifact consistency

## Outcome

Make schemas, Rust types, protocol examples, CLI capability output and documentation mechanically agree.

## Dependencies and ownership

Dependencies: AR-0101, AR-1001.
Owned paths: schemas, API snapshots, conformance corpus and generated documentation.
Branch: feature/contract-consistency. Worktree: agent-systems-benchmark-contract-consistency.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Define canonical sources and generated/artifact-diff gates before extension implementations fan out.
2. Implement the smallest complete boundary and its independent failure tests.
3. Run focused and applicable full quality gates on the exact commit.
4. Publish a focused PR, reconcile CI and record verified limitations.

## Acceptance criteria

Canonical schemas validate positive/negative corpora; Rust serialization round-trips every example; incompatible schema/API changes fail semantic compatibility checks; generated capability/support tables have zero diff; JSON-RPC stateful conformance covers negotiation, bounds, cancellation and errors.

## Evidence and completion

Record source/tool versions, commands by digest, immutable commit and CI identifiers,
observed outcomes and remaining limitations. No raw private configuration or transcripts.
Include negative cases and a real production-boundary integration where applicable.
Do not satisfy native evidence with mocks or emulation. Finish only after the requested
outcome, review, exact-head checks and post-merge checks pass.
