# AR-1291: Repair superseded dependency pointers

## Outcome

Repair missing successor pointers on the immutable superseded tmux recovery tasks so the
coordinator can validate dependency completion without treating superseded work as silently done.

## Scope

State repository metadata only. Add and validate the exact chain `AR-1052 -> AR-1054 -> AR-1056 ->
AR-1058 -> AR-1061 -> AR-1062`. Do not change product code, handoffctl implementation, generated
views by hand, or historical task evidence. Use signed state commits and preserve all existing
task revisions/evidence.

## Acceptance

- Each pointer is hash-independent, schema-valid, and terminates at done AR-1062.
- `handoffctl doctor --live`, reconcile, and generated status checks pass.
- AR-1010 can be claimed or explicitly blocked/released by normal workflow afterward.
