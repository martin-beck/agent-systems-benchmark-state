# AR-1065: Normalize asb-tui branch required-status policy

## Evidence

On 2026-09-14, `asb-tui` PR #26 at `58cf344a6bc5c45aa7e48577fa08b598ab6446bc` had a successful
GitHub Actions check-suite (`Rust, supply-chain, and privacy gates`, app id 15368), while the
legacy commit-status API reported no statuses and `mergeStateStatus` remained `BLOCKED`. PRs #24
and #25 show the same repository-wide condition.

## Authorized repair

Using repository administration, retain the app-scoped required check and remove the duplicate
legacy `contexts` requirement. Do not disable branch protection, bypass checks, post synthetic
statuses, or alter source/CI files. Re-evaluate the existing green PR and rerun its check only if
GitHub requires it.

## Acceptance

The existing green PR becomes normally mergeable under protected policy; branch protection remains
enabled and the app-scoped check is still required. Record before/after policy JSON and PR merge
evidence without publishing private data.
