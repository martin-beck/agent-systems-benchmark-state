# AR-1182: Repair coordinator vendor snapshot integrity

## Outcome

Restore one complete byte-exact Agent Workflow Coordinator vendor snapshot from a reviewed,
cryptographically signed upstream release. Keep fail-closed digest verification mandatory and
unblock coordination-content PRs only after exact-head and post-merge verification pass.

## Dependency and ownership

Dependency: AR-0851, which established the pinned offline vendor contract and is done.

Repository: `martin-beck/agent-systems-benchmark-state` only. Owned paths are the complete file set
declared by `tools/handoffctl_vendor.py`, `coordinator.vendor.json`, directly corresponding vendor
tests/documentation, and the path-filtered Coordination verification workflow if an upstream release
requires it. No ASB product, `asb-tui`, task-status semantics, renderer policy, privacy policy,
signature policy, DCO policy, or unrelated generated view is owned.

AR-1063 state PR #20 is blocked on this repair because every coordination-content PR executes the
pinned vendor verifier before schema/DCO checks. AR-1182 must not absorb or modify AR-1063 scope.

## Exact current mismatch and provenance

- Lock: Agent Workflow Coordinator `v0.3.5`, signed upstream commit
  `510817b93feb80dde13e5a6c61d657954fae2346`.
- Locked `tools/handoffctl.py` SHA-256:
  `ccc2145c2d6345d93648566c66c79d9d1e7fe32a1ee2c52fd79e455220325f10`.
- Checked-in `tools/handoffctl.py` SHA-256:
  `8d9a4304de2f7d7a074508c2232a8853864f94c2a9c1ee0a1df06c4632dfca2f`.
- `python3 tools/handoffctl_vendor.py verify --target .` fails first and exactly with
  `vendor digest mismatch: tools/handoffctl.py`; all other checked file hashes equal the lock when
  audited independently.
- Signed+DCO downstream commit `4915e9c9a4f58f94ac013f40f1d9f4f982321faf` added four
  `recover-expired` filtering lines directly to the vendored file after the v0.3.5 snapshot. The
  behavior addressed a real liveness issue, but an unpinned downstream edit violates the snapshot
  contract.
- Upstream main contains broader reviewed lifecycle-recovery work at
  `47dcee1` (`fix(git): keep lifecycle recovery available`), but no immutable signed release newer
  than v0.3.5 is presently available. An untagged branch or main commit is not a valid vendor source.
- State PR #20 run `34779682318` failed only at `Verify pinned coordinator release` with this exact
  mismatch; its AWQ shadow check passed.

## Work sequence

1. Obtain or publish through the upstream repository's protected process a cryptographically signed
   immutable release newer than v0.3.5 that contains the required independent expired-lease recovery
   behavior and all associated tests/formal evidence. Do not treat current upstream main as a release.
2. Verify the release tag signature, exact commit signature and DCO, version, changelog, complete
   upstream tests, formal models, and release CI. Record immutable tag and commit identities.
3. From a clean checkout of that exact tag, use only `tools/handoffctl_vendor.py sync`; do not copy,
   patch, regenerate, or edit individual vendored files or lock digests manually.
4. Require the sync to replace the complete declared vendor set atomically and generate every digest
   from source bytes. Reject missing, extra, symlinked, non-regular, or locally modified sources.
5. Run offline `handoffctl_vendor.py verify`, all coordinator fault/race/storage/renderer tests with
   coverage, formatting, Ruff, mypy, schema/graph/privacy/generated-view checks, formal models,
   source headers, and clean-tree checks.
6. Add negative tests proving the original four-line downstream divergence and any one-byte vendor
   or lock mutation fail verification. Do not add allowlists or mismatch exceptions.
7. Obtain independent immutable review, publish one signed+DCO state PR, require Coordination
   verification and AWQ shadow checks green, merge through the protected path, and rerun exact-main
   offline vendor verification and full state gates.
8. Rebase/re-run AR-1063 PR #20 only after this repair is on protected main. Do not merge or bypass
   AR-1063 while its vendor gate is red.

## Acceptance

- Every declared vendor destination digest equals both the lock and the exact signed upstream-release
  source byte; the vendor verifier exits zero offline on a clean tree.
- No vendored destination differs from the selected release and no downstream-only behavior remains.
- Required lease recovery remains available through upstream-reviewed implementation and tests.
- One-byte changes to any vendor file or digest fail closed; CI has no exemption, skip, fallback,
  mutable-ref fetch, or network dependency during verification.
- Exact-head review and CI, protected merge signature/DCO, and exact-main verification are green.
- No product, TUI, task-state, renderer, privacy, signature, or DCO policy is weakened or changed.

## Evidence to record

Record upstream release/tag/commit signatures, DCO and workflow identities; old and new complete
manifest digests; sync command and clean-source identity; offline positive and hostile mutation
results; complete test/formal/coverage results; independent review identity; state PR head/tree and
checks; protected merge metadata; and exact-main vendor verification.
