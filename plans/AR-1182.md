# AR-1182 plan: coordinator v0.3.6 supersession vendor sync

1. Work in an isolated clean state worktree; inspect current vendor drift and preserve unrelated changes.
2. Fetch `v0.3.6`, verify the annotated SSH signature and exact peeled commit `a1bc4459f884ce447e8ee2884df12ea3ff4b710b`.
3. Run the upstream `tools/vendor.py sync` contract against the exact tag; do not hand-edit vendored files or the lock.
4. Review the complete allowlist diff, confirm the old downstream `handoffctl.py` patch is removed, and verify manifest digests/version.
5. Run `handoffctl_vendor.py verify`, state unit/schema/generated-view/privacy checks, and applicable formal/integration tests.
6. Commit only the vendor snapshot and required downstream assertions/documentation, signed with DCO; obtain independent exact-head review and required CI.
7. Record immutable evidence, then release this AR only after review; do not change ASB/asb-tui product sources.
