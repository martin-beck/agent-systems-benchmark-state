# AR-0518 plan

1. Capture and document exact pinned Gemini 0.58.0 request/response facts without changing shared replay paths.
2. Implement only the bounded ProviderDialect, cassette/schema, fixture, and StrictReplayService changes listed by the AR.
3. Add strict positive and fail-closed malformed/tool/stream/query/model negatives.
4. Run full workspace, formal, privacy, fuzz/mutation, x86_64, and aarch64 gates plus a real loopback replay.
5. Obtain independent review, exact-head CI, signed/DCO merge, release, and update AR-0510 acceptance.
