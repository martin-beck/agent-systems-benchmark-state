# AR-0880: Repair OpenJiuwen runtime closure

## Outcome

Replace the incomplete OpenJiuwen runtime lock with a closed, hash-pinned Python 3.11 Linux x86_64
closure for the exact `openjiuwen[cli,observability]==0.1.17.post1` wheel. Prove that the exact
wheel, imports used by noninteractive execution, and console entry point load from a clean offline
installation. This repairs provenance only; AR-0859 retains ownership of live provider, editing,
tool, usage, cancellation, cleanup, and network-denial evidence.

## Dependencies and ownership

Dependency: AR-0857. AR-0859 is the consumer and supplied the immutable defect evidence; this AR
must complete before AR-0859 resumes, so it intentionally does not depend on AR-0859.

Branch: `fix/openjiuwen-runtime-closure`. Worktree:
`agent-systems-benchmark-openjiuwen-runtime-closure` under `/srv/data/projects`.

Owned product paths:

- `crates/asb-agents/tests/fixtures/openjiuwen-runtime.lock`
- `crates/asb-agents/tests/fixtures/openjiuwen-provenance.json`
- `crates/asb-agents/tests/openjiuwen_runtime_closure.rs`

The Python lock and provenance-fixture fence is exclusive to this repair. Root `Cargo.toml`,
`Cargo.lock`, schemas, adapters, and AR-0859's three live-test paths are out of scope. If a Cargo
edge becomes necessary, stop and obtain a separate serialized Cargo fence before mutation.

## Exact repair boundary

- Preserve OpenJiuwen version `0.1.17.post1`, wheel SHA-256
  `21e9479c6b858cda28c250d63066f862fc0915cf2039edb00f016cbec7f9abba`, source revision
  `4d56dad14b67cdb8fbb3fe58a5654ac8c3e3b815`, license receipt, and Python/platform boundary.
- Resolve exactly `openjiuwen[cli,observability]==0.1.17.post1` with the pinned resolver and record
  the resolver version, command, complete hashes, package count, and resulting lock digest.
- Require exact direct packages `prompt-toolkit==3.0.53` and `opentelemetry-sdk==1.44.0` selected
  by the reviewed resolution. Bind every added transitive artifact by version and hash; no floating
  requirement, ambient index state, editable install, unverified wheel, or private cache qualifies.
- Install into two new private environments using hashes, first with network acquisition into a
  bounded cache and then offline from that verified cache. Require identical installed
  name/version inventories and preserve no credentials or private paths in public evidence.

## Tests and hostile cases

- Verify the provenance JSON names the exact wheel and repaired lock digest and that the lock input
  explicitly includes both `cli` and `observability` extras.
- From a clean synchronized environment, import `prompt_toolkit`, `opentelemetry.sdk`,
  `openjiuwen.harness.cli.ui.runner`, and the recorded console-script module, then require
  `openjiuwen --version`, `openjiuwen --help`, and `openjiuwen run --help` to terminate successfully.
- Prove the entry point reaches a bounded loopback connection attempt without any missing-module
  failure; do not call this a successful live session and do not contact a non-loopback endpoint.
- Reject deletion, version drift, digest mutation, extra removal, base-only resolution, absent or
  swapped wheels, unpinned resolver identity, network use during the offline install, partial cache,
  ambient site packages, user configuration, and secret-shaped environment values.
- Assert diagnostics and committed fixtures contain no credential, authorization value, prompt,
  response, user name, machine identity, or host-private path.

## Gates and completion

Run focused closure/import/entrypoint tests, workspace format, Clippy, locked tests, docs and release;
repository policy, dependency/license/SBOM checks, privacy/Gitleaks, fault/formal gates, and
applicable x86_64/aarch64 CI. Record the exact old and new lock digests and package delta. Publish
only an independently reviewed SSH-signed+DCO exact tree. On completion, AR-0859 may resume its
separate executable loopback qualification; this AR alone makes no provider or support claim.
