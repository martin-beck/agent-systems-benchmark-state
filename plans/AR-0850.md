## AR-0850 plan

Use the pinned aider 0.86.2 fixture and preserve the exact nondeterministic diagnostic.
Implement the smallest deterministic adapter/runtime control, test multiple separately
spawned processes with multiple editable files, verify strict replay and privacy/fault
negatives, then run all applicable Rust, quality, formal, fault, coverage, supply-chain,
and native replay gates. Publish only a signed focused candidate with independent review.
