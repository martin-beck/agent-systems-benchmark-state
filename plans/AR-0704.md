# AR-0704 plan

Implement only after explicit authorization for disposable native x86_64 capacity on the
authorized development host exists. The controller must enforce a local resource budget,
exclusive expiring leases, least-privilege credential handling, provenance, teardown, and
orphan recovery. No external provider account or native ARM64 capacity is required for this
AR; native ARM64 is optional future evidence and must not block it.
