# AR-1056: Diagnose trusted tmux socket connection stage

## Outcome

Identify and repair the exact AF_UNIX connection subpredicate that still fails all five live tmux
fixtures on trusted main after AR-1054 added Linux `EAGAIN` handling and authoritative `SO_ERROR`
completion. Preserve all authenticated cleanup authority and change no product UI or runtime code.

## Scope and ownership

Recovery parent: AR-1054. Exact asb-tui base:
`092cf20a274e11f2155116a1b27af549e4e35f9e`, tree
`ff609b21e655b30a06cf5ccc6848ad239574c812`. Repository: `martin-beck/asb-tui` only. Owned path:
`tests/terminal_foundation.rs`. Branch: `fix/tmux-socket-connect-diagnostics`. Worktree:
`agent-systems-benchmark-asb-tui-tmux-socket-connect-diagnostics`. No renderer, application,
lifecycle protocol, dependency, release, workflow or ASB source change.

AR-1054 remains open pending this recovery rather than becoming a reverse dependency. AR-1010 is
explicitly gated on AR-1056 and exact-main qualification; AR-1056 has no dependency on AR-1010, so
the graph remains acyclic.

## Exact failure evidence

AR-1054 PR #20 exact-head Repository quality run `34563473057` passed. GitHub-verified squash
`092cf20a274e11f2155116a1b27af549e4e35f9e` has exact reviewed tree, parent `7a398032`, and a valid
raw multiline DCO trailer. Trusted-main run `34563628936` failed 24 of 29 terminal tests: all five
live tmux fixtures still exhausted at the aggregate closed stage `socket_connection_unavailable`.
This proves the earlier `EAGAIN`/premature `POLLHUP` diagnosis was insufficient, while peer
credential, generation and later authority stages remain unreached. Postmerge Repository quality
run `34563629050` separately failed an unchanged system-probe resize assertion; record and rerun it,
but do not mix that unrelated flake into this socket-only diff.

## Diagnostic and repair sequence

1. Split only `socket_connection_unavailable` into fixed bounded privacy-safe substages: path
   invalid, socket creation unavailable, immediate connect error outside the accepted incomplete
   classes, poll unavailable, poll timeout, poll invalid descriptor, poll no completion event,
   `SO_ERROR` unavailable, `SO_ERROR` malformed length, and nonzero `SO_ERROR`. Never emit errno,
   flags, path, PID, UID, session, raw output or timing.
2. Add pure injected fixtures covering every substage, direct success, `EINPROGRESS`, AF_UNIX
   `EAGAIN`, readiness combinations, and transitions. Preserve existing NUL/overlong path and peer
   credential tests.
3. Merge only the approved diagnostic checkpoint and use exact trusted-main evidence to select a
   repair. Do not weaken a predicate speculatively.
4. Repair only the proven subpredicate. Retain owner/type/dev/inode socket bracketing, exact peer
   PID/start-time, repeated identity equality, stable startup and every pre-option/pre-TERM/
   pre-KILL revalidation. Missing or changed authority performs zero option, signal, server kill or
   unlink.

## Tests and gates

Run repeated serial and parallel terminal suites with scoped process/server leak checks, format,
Clippy, locked tests, rustdoc, release, coverage, cargo-deny/audit, schemas, channel/publication,
promoted self-test, shell/workflow, privacy, signature/DCO and clean tree. Each diagnostic and any
subsequent repair checkpoint requires fresh immutable review, exact-head CI and separately
authorized protected integration. Final acceptance requires both exact-main Repository quality and
Trusted main verification green at the exact merge.

## Acceptance

Trusted diagnostics expose one closed connection substage without private values; the exact repair
then makes all five live tmux fixtures pass without weakening authority. No owned process or live
server leaks, all gates are green, and the final diff remains one test-only file with no product
UI/render work.
