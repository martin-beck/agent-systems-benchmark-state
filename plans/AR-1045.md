# AR-1045: Attest the Gemini readiness merge boundary

## Outcome

Restore a green protected `main` after GitHub's default merge for PR #136 preserved the reviewed
tree and a valid GitHub signature but omitted the required matching DCO trailer.

## Immutable boundary to attest

- PR: `martin-beck/agent-systems-benchmark#136`, merged at `2026-09-11T01:23:40Z`.
- Reviewed base: `44eb1b48cb79b789252eff1cc798980d868c908c`.
- Reviewed signed+DCO head: `55648d5a29f4c29e9525eb7f2a890ac5232d7b5a`.
- Reviewed and published tree: `e7eb2b713de8abaf4af5d75f8882622740c7c3d8`.
- Published merge: `2ecb876b82a91a8103926b298c42ad49ce8dd143`, with first parent
  `44eb1b48cb79b789252eff1cc798980d868c908c` and second parent
  `55648d5a29f4c29e9525eb7f2a890ac5232d7b5a`.
- GitHub verification: `verified=true`, `reason=valid`.
- Reviewed `crates/asb-agents/src/gemini.rs` SHA-256, equal in the merge tree:
  `fdffabc78b6b76da0dd3e22729cab756925bc7c0edb68178845b11df04e03b17`.
- All 12 PR exact-head checks completed successfully. The push workflow Repository quality run
  `34550483000` then failed exactly because merge `2ecb876` lacks a matching `Signed-off-by`
  trailer. The attestation must not claim that this historical merge is DCO-compliant.

## Dependencies and ownership

No implementation dependency. This is a focused forward-only recovery for AR-1041 in
`martin-beck/agent-systems-benchmark`. Owned product paths are one bounded machine-readable merge
attestation, its closed validation/negative test, and the minimum durable publication documentation
needed to prevent recurrence. It owns no Gemini behavior, protocol, renderer, Ratatui, Crossterm,
terminal lifecycle, widget, screen, selection, search, or other TUI/frontend state.

Branch: `docs/gemini-readiness-merge-attestation`. Worktree:
`agent-systems-benchmark-gemini-readiness-merge-attestation`.

## Work sequence

1. Capture the immutable facts above plus the exact 12 successful PR check names/run URLs, GitHub
   verification payload/time, raw reviewed-head DCO trailer, and raw missing-DCO merge message.
2. Add a bounded closed-schema attestation and deterministic validation that rejects unknown fields,
   malformed hashes, false signature/DCO claims, wrong parents, wrong tree, mismatched Gemini source
   content, missing checks, duplicate checks, and oversized input before attacker-controlled growth.
   Reuse the existing AR-1039 attestation pattern where compatible; do not alter runtime behavior.
3. Add or extend a durable policy/regression surface that makes the publication command exact:
   `gh pr merge --merge --subject ... --body ...`, where the body is genuinely multiline and ends
   with `Signed-off-by: martin-beck <martin.beck2@gmx.de>` matching GitHub's merge author. Default
   `gh pr merge --merge`, rebase, squash, escaped-literal newlines, and title-cased `Martin Beck`
   trailers are invalid for protected publication.
4. Publish a locally SSH-signed topic commit with a matching source DCO trailer. Freeze its exact
   head/tree for independent immutable review and require all 12 exact-head checks to succeed.
5. Merge only with `--merge`, an exact subject, and an actual multiline merge body ending in the
   lowercase GitHub-author trailer above. Verify the corrective merge has two expected parents,
   exact reviewed tree, GitHub `verified=true/reason=valid`, and a raw matching DCO trailer.
6. Run and record every post-merge `main` workflow. Do not release AR-1041 or AR-1045 until current
   protected main is terminal green; never rewrite history to conceal `2ecb876`.

## Acceptance criteria

The historical non-DCO merge remains visible and is accurately classified; the attestation binds
all listed identities, tree/content hashes, verification facts, and exact-head checks; malformed and
false claims fail closed within explicit bounds; the corrective source and GitHub merge commits are
both signed and DCO-compliant; current `main` required workflows are green; product semantics and the
Gemini tree remain unchanged except for bounded attestation/documentation/testing; and no UI/TUI
ownership enters ASB.

## Evidence

Record exact commit/tree/parent hashes, file SHA-256, GitHub verification JSON, raw commit trailers,
all 12 original and corrective exact-head checks, the failed `34550483000` policy classification,
bounded positive/negative attestation tests, full applicable quality/privacy gates, independent
review, exact merge command, and every post-merge main workflow result.
