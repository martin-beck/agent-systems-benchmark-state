# AR-1014 plan

Repository: `martin-beck/asb-tui`. Consume the reviewed ASB catalog through the standalone TUI
protocol. Implement grouped selection,
validation, presets, resize-safe rendering, and deterministic plan serialization with focused and
negative tests before exact-head CI.
