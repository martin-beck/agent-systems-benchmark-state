# AR-1051: Attest the measurement catalog merge boundary

## Outcome

Make the PR #140 publication boundary independently auditable without altering history or changing
the already reviewed catalog implementation. Record the exact head, tree, merge, parents,
signature, checks, post-merge outcomes and DCO mismatch, then enforce the correct future merge
identity in executable tests and documentation.

## Scope and repository boundary

Repository: `martin-beck/agent-systems-benchmark` only. Allowed changes are a bounded public
attestation fixture, its validation test and narrowly related publication/quality documentation.
No production source, protocol behavior, catalog schema, TUI, renderer, Ratatui or Crossterm code.
All actual frontend work remains exclusively in `martin-beck/asb-tui`.

## Immutable evidence

1. Bind PR #140 base `23530dfc808650a8f3019c87a1c69fe3d0d654b5`, reviewed head
   `8e8d37e8c7ca7a4f673e8ae84d7395d512d45197`, reviewed tree
   `733ecc72586e1d395b45962083fa60ff6a5c258b` and merge
   `252f746e903555c2dc626fadfa1a75bb76913144` with exact two-parent topology and tree equality.
2. Verify the reviewed head signature/DCO and all 12 exact-head successful checks from immutable
   GitHub URLs. Record all seven protected-main workflow outcomes for the merge, including the
   exact repository-quality failure and the successful formal lockfile qualification.
3. Record the merge's valid GitHub signature and exact author
   `martin-beck <martin.beck2@gmx.de>`. Assert that its raw trailer
   `Signed-off-by: Martin Beck <martin.beck2@gmx.de>` does not match the author name and therefore
   `merge_commit_dco` is false. Never present a later green descendant as retroactive compliance.
4. Read any historical Git object through an explicit pre-allocation byte cap, suppress or bound
   stderr, terminate and reap on every error, and reject substitutions, missing objects and excess.

## Corrective recipe and tests

1. Require protected merge commits with real multiline bodies whose final trailer is exactly
   `Signed-off-by: martin-beck <martin.beck2@gmx.de>`. Reject title-cased name variants, escaped
   newline text, squash/rebase/default merge methods and missing or duplicate trailers.
2. Use a closed schema with strict byte/count/text/URL bounds and no private environment data.
   Add negatives for every identity, parent, tree, check, workflow, signature and DCO claim.
3. Run focused tests plus full format, lint, workspace, documentation, release, contract,
   supply-chain, formal and repository-policy gates. Freeze a signed conventional DCO commit for
   immutable different-agent review before publication.
4. Require all exact-head PR checks and all protected-main workflows. Verify the resulting merge
   parents/tree/signature/raw author and exact matching lowercase-key trailer before marking done.

## Dependencies and acceptance

AR-1045 must first restore a green protected-main descendant using the corrected exact author
trailer. AR-1051 is accepted only when its evidence is independently reproducible, all false claims
fail closed, its own merge is DCO-compliant, and every post-merge workflow is green. This task
attests history; it does not authorize any UI implementation in ASB.
