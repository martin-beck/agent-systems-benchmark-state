# AR-0853: Repair coordinator merge attestation

## Scope

- Preserve PR #10, merge `e52ce3aaa59ffc4cc6f97657b6ea2c7dfceb2ac1`, and every published ref without rewriting history.
- Establish an explicitly reviewed signed+DCO replacement or additive attestation for the v0.1.4 coordinator merge.
- Bind the repair to the exact merge parents, tree, PR head, upstream release, and vendor manifest.

## Acceptance

- The repair preserves the existing merge and all unrelated state history.
- A locally verifiable Martin Beck SSH signature and matching Signed-off-by trailer cover the exact v0.1.4 tree and immutable merge evidence.
- Documentation states precisely what the additive repair proves and does not falsely claim that the original merge commit itself acquired a new signature or trailer.
- The v0.1.4 vendor verifier, binding rejection tests, full coordinator contract suite, formal verification, snapshot, and live doctor pass.
- The focused repair has exact-head reviewed CI and a clean synchronized post-merge state.

## Out of scope

- Rewriting, deleting, or replacing existing public refs.
- Coordinator runtime changes unrelated to merge attestation.
- Benchmark product code.
