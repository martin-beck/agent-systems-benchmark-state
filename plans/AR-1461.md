# AR-1461: First-customer release readiness and publication

## Required work

1. Pin exact qualified main `36d4bdf35a644a36a8acfdb31078eb7f668a17c4` and
   verify a clean isolated release worktree.
2. Determine the repository's established version/tag and bundle workflow;
   do not invent a release format or replace existing gates.
3. Build and inspect the release bundle, checksums, provenance, privacy,
   source/header, formal, and reproducibility evidence.
4. Verify the current-main first-customer qualification is the exact release
   input, publish only after all required hosted checks are green, and verify
   the public tag/release and fresh local consumption.
5. If any gate fails, preserve the failure and create a narrowly scoped repair
   AR instead of forcing publication.
