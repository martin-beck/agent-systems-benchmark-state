# AR-0309: Implement maintained OpenHands SDK client adapter

## Outcome

Run a pinned MIT OpenHands SDK or maintained canonical headless client through structured output.

## Dependencies and ownership

Dependencies: AR-0101, AR-0102, AR-0103.
Owned paths: asb-agents OpenHands module and its fixtures.
Branch: feature/agent-openhands. Worktree: agent-systems-benchmark-agent-openhands.
One worker owns this task; shared workspace/schema edits require coordinator integration.

## Work sequence

1. Resolve the maintained official OpenHands SDK/client boundary, exact source and license; do not adopt the retired standalone CLI or enterprise-licensed code as an unqualified dependency.
2. Pin its Python dependency graph and implement a bounded subprocess or ASB JSON-RPC plugin boundary with isolated settings, tools, persistence and explicit provider/base URL.
3. Exercise real headless JSON completion, edits, tool calls, cancellation and failure behavior against the common credential-free loopback fixture.
4. Run focused and full gates, publish a focused PR, reconcile exact-head CI and record maintenance/licensing limitations.

## Acceptance criteria

A maintained real MIT-licensed OpenHands boundary completes the common offline fixture with bounded structured lifecycle/tool/usage evidence; endpoint override, config isolation, no-persistence mode, action approval policy, cancellation and cleanup are verified. Retired CLI, cloud, enterprise and untested container/platform combinations are explicitly excluded.

## Evidence and completion

Record exact source/tag/tree, per-path license review, Python lock/package or image integrity, commands by digest, immutable commit, CI identifiers, native outcomes and unsupported combinations. No credentials, raw conversations, diagnostics, prompts or private paths. Finish only after independent immutable-head review, exact-head checks, integration and post-merge verification.

