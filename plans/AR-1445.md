# AR-1445: Protected-main topology repair for OpenRouter refresh

## Objective

Repair the single-parent merge of PR #323 through a normal reviewed
non-squash descendant merge, without weakening repository policy or changing
the already-qualified OpenRouter behavior.

## Required evidence

1. Preserve failed post-merge run `36144090160` and merge `84b587ec` as
   immutable evidence.
2. Produce a signed+DCO topic commit and a merge commit with exactly two
   parents: current protected main as first parent and the reviewed topic as
   second parent; verify tree equality and exact-head checks.
3. Run all seven post-merge workflows on the exact repair merge and require
   terminal success.
4. Reconcile state and release AR-1440 only after the repaired protected-main
   policy passes.

## Boundaries

Do not rewrite or force-push protected history, skip policy, alter CI to hide
the failure, resume blocked AR-1329, or persist live credentials, prompts,
responses, or private paths.
