# AR-1010: Adopt Ratatui/Crossterm TUI foundation

## Outcome

Make asb-tui a real, maintainable terminal application using pinned Ratatui and Crossterm while keeping control, runner, protocol, and benchmark state outside the renderer.

## Dependencies and ownership

Dependencies: AR-0803, AR-0804, AR-0805, AR-0806.
Owned paths: crates/asb-tui, workspace dependency manifests, terminal lifecycle, event loop, renderer shell, and TUI unit/snapshot fixtures.
Branch: feature/tui-ratatui-crossterm-foundation. Worktree: agent-systems-benchmark-tui-ratatui-crossterm-foundation.

## Work sequence

1. Pin the Ratatui release and matching Crossterm version approved for the Rust 1.93 workspace; inspect licenses and lockfile changes.
2. Build a single-writer application shell with Action messages, deterministic AppState updates, and a pure state-to-frame renderer.
3. Implement safe raw-mode/alternate-screen setup and restoration, panic/error cleanup, resize handling, Ctrl-C, SSH/tmux/screen fallbacks, and NO_COLOR behavior.
4. Connect negotiated ASB control events without moving runner ownership or benchmark work into the UI process.
5. Add TestBackend frame tests, terminal lifecycle tests, and plain-text rendering equivalents before adding visual polish.

## Acceptance criteria

The standalone TUI launches into a real Ratatui screen, remains responsive while control operations run, restores the terminal on every tested exit path, renders a useful compact/plain fallback, and cannot alter benchmark results or bypass explicit confirmation. No second incompatible Crossterm version is introduced.

## Evidence

Record exact dependency versions, lockfile/license audit, immutable commit, unit and snapshot results, terminal restoration probes, native Linux/Windows and SSH/multiplexer checks, independent review, exact-head CI, and post-merge verification.
