# AR-0320: Integrate provider credential boundary

## Outcome

Bring the reviewed AR-0318 environment-only resolver into product `main` with serialized Cargo/workspace integration, preserving its signatures, DCO, privacy boundary, and explicit FD/helper rejection.

## Dependencies and ownership

Dependency: AR-0318. Owned paths: product workspace integration, Cargo metadata/lockfile, and integration fixtures/tests required to expose the already-reviewed credential module. Branch: `feature/provider-credential-integration`. Worktree: `agent-systems-benchmark-provider-credential-integration`.

## Acceptance criteria

1. Integrate only the reviewed signed AR-0318 source and required minimal workspace wiring; do not rewrite unrelated history or weaken the credential boundary.
2. Run locked workspace build/test, formatting, clippy, privacy/dependency checks, and exact-tree provenance/signature/DCO checks.
3. Prove a synthetic environment-credential preflight can run on the exact integrated product tree without exposing credential bytes; preserve fail-closed FD/helper behavior.
4. Publish only after independent review, exact-head CI, and post-merge verification. Record the integrated immutable product commit and all unsupported combinations.
