## AR-0848 plan

Authorization: user explicitly authorized disposable native x86 capacity using the existing
development host and deferred native aarch64. Use only the existing x86_64 host, with no external provider
or aarch64 provisioning. Prove fail-closed labels, isolation, cleanup, provenance, and
redacted evidence before publication.
