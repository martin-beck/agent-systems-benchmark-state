# AR-1264: Recover signed integration after PR #205

## Outcome

Restore the protected-main signature and DCO boundary after GitHub merged PR #205 as unsigned
merge `ebfa37023e56269b8299254b535be60f3cf1d186`, without rewriting published history, and
re-verify the AR-1263 change on the resulting exact main.

## Dependencies and ownership

Dependency: AR-0897. AR-1263's merged feature and unsigned merge are preserved as the incident
evidence; its blocked post-merge record must not prevent this independent recovery. Owned paths:
signed forward-only recovery evidence and integration policy.
Branch: `repair/ar-1264-pr205-merge-integrity`. Worktree:
`agent-systems-benchmark-ar-1264-merge-integrity`.

## Work sequence

1. Preserve the published merge commit, exact parents/tree, feature head, and failed DCO evidence.
2. Create a minimal SSH-signed, DCO-bearing forward-only recovery commit/merge using the reviewed
   integration helper through `handoffctl run`; never rewrite or replace published history.
3. Verify exact main ancestry, tree, signature, DCO, privacy, and all required exact-main and
   post-merge workflows. Add or update policy evidence only if required to prevent recurrence.
4. Obtain independent exact-head review before publication and release this AR only after every
   required check is terminal green.

## Acceptance criteria

The unsigned historical merge remains documented; current protected main has a signed DCO-bearing
recovery boundary, exact-main policy and post-merge assurance are green, and AR-1263 remains
traceable to its reviewed feature tree. No gate is weakened and no history is rewritten.

## Evidence

Record immutable refs, parents, trees, SSH signatures, DCO, workflow IDs/conclusions, clean
worktree state, independent review, and durable next action.
