# AR-0520 plan

1. Preserve Goose’s real mixed GET/POST evidence without logging private values.
2. Implement per-interaction request redaction selectors in schema, sealing, and strict matching.
3. Add mixed GET-null/POST-private fixtures and fail-closed missing-selector, injection, and mismatch tests.
4. Run full workspace, formal, privacy, fuzz/mutation, native x86_64 and pinned QEMU-emulated AArch64 gates; native ARM64 is optional and non-blocking.
5. Independently review, merge, release, and resume AR-0512.
