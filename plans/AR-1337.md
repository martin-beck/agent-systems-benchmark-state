# AR-1337: Repair protected-main merge-tree admission after OpenRouter merge

## Outcome

Restore truthful protected-main merge-tree verification after post-merge run
35825987339 rejected merge `56c882a06337a2b58c761bddaef2c743cba65712`.

## Work sequence

1. Reconcile state and inspect the exact PR base, topic head, merge parents,
   GitHub merge commit and computed trees.
2. Reproduce the failure locally with the repository policy in protected-main
   mode and determine whether GitHub merge semantics or the verifier is wrong.
3. Implement the smallest reviewed repair in the correct ASB workflow/policy
   boundary. Preserve SSH signature, DCO, exact-head and no-bypass checks.
4. Add positive and hostile regression fixtures for stale base, altered merge
   tree, wrong parents and off-tip merges.
5. Run focused and full applicable gates, independent review, exact-head CI,
   then verify every post-merge workflow on the repaired protected main.

## Acceptance and evidence

The reviewed topic tree and protected-main merge tree must be identical for the
authorized merge, with immutable parent/head/tree IDs and terminal-success
post-merge workflows. No hash-specific exception or skipped required check is
acceptable.

## Current development and CI qualification boundary

The mandatory development and CI qualification path for this AR is a deterministic
local provider/LLM mock (LiteLLM-compatible where practical), including hostile
negative tests and offline replay where applicable. External/live OpenRouter or
other provider reachability is optional supplementary evidence only; it is never a
completion, dependency-readiness, or CI gate. Production egress policy, credential
non-disclosure, runtime-owned authority, namespace/relay attestation, cancellation
and teardown, and fail-closed denial of unapproved external traffic remain required
contracts. Existing live-provider dependency edges describe production integration
ordering only and must not be used to block local qualification or to claim external
reachability.
